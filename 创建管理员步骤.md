# Supabase 创建管理员账号步骤

## 方法一：使用 Supabase 控制台（推荐）

### 步骤：

1. **登录 Supabase**
   - 打开 https://supabase.com/dashboard
   - 进入你的项目

2. **找到 Authentication**
   - 左侧菜单 → **Authentication** → **Users**

3. **创建新用户**
   - 点击右上角 **Add user** 或 **Create user** 按钮
   - 填写以下信息：

   | 字段 | 值 |
   |------|-----|
   | Email | `admin@admin.local` |
   | Password | `admin123` |
   | Confirm Password | `admin123` |
   | Auto Confirm User | ✅ 勾选 |

4. **点击 Create**

---

## 方法二：使用 SQL 创建（如果方法一不行）

打开 SQL Editor，执行：

```sql
-- 方式1: 使用 RPC 函数创建用户
select auth.sign_up(
  email:='admin@admin.local',
  password:='admin123',
  data:='{"role": "admin"}'
);

-- 方式2: 直接插入 auth.users 表（需要配置邮件发送）
-- insert into auth.users (email, encrypted_password, email_confirmed_at)
-- values (
--   'admin@admin.local',
--   crypt('admin123', gen_salt('bf')),
--   now()
-- );
```

---

## 方法三：后台直接注册

1. 打开你的后台页面：https://faca168.github.io/recharge-h5/admin.html
2. 在登录页下方是否有"注册"链接？
3. 如果有，点击注册账号

---

## ⚠️ 注意

- 如果你开启了 Supabase 的邮件确认，需要先去邮箱点确认链接
- 创建完成后，用 `admin` 和 `admin123` 登录

---

## 验证是否创建成功

创建后，在 Supabase 控制台 → Authentication → Users 应该能看到 `admin@admin.local`

---

**你用的是哪种方法？告诉我结果！**